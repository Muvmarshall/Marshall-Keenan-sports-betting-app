PORT=4000
DATABASE_URL=postgres://parlay:parlay@localhost:5432/parlay
ENABLE_POLLER=true
POLL_INTERVAL_MS=60000
ODDS_PROVIDER=mock

# To switch to real odds (server/src/odds/theOddsApiProvider.ts), set:
#   ODDS_PROVIDER=live
#   ODDS_API_KEY=<your key from the-odds-api.com>
#   POLL_INTERVAL_MS=21600000   # 6 hours — see README "Live odds feed" for why
